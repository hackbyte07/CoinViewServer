ktor {
  deployment {
    port = 8080
    port = ${?PORT}
    //host = localhost
  }
  application {
    modules = [com.hackbyte.ApplicationKt.module]
  }
}
jwt {
  domain = "8080"
  audience = "jwt.audience"
  issuer = "jwt.issuer"
  realm = "coin-view"
}
